Person 1 – Mira
• Built all 4 HTML pages: Home (index.html), Plan Details (plan-details.html), Feedback/Support (feedback.html), and Profile (profile.html).
• Created consistent navigation, header, and footer across all pages.
• Added favicon and footer year auto-update.
• Linked all pages together.
• Set up the full folder structure for the project (assets/css, assets/js, assets/img).
• Prepared the project for CSS and JS integration.
• Completed one required form (Contact / Support form).
• Verified semantic HTML and navigation.

Person 2 – Leen (JavaScript)
• Create dummy data file: assets/js/data.js.
• Create app logic file: assets/js/app.js.
• Add 3 forms inside profile.html or a new page:
 – Add User form (at least 4 input types such as text, email, password, date).
 – Add Plan form (at least 4 input types).
 – Search form + results table (search pets from dummy data).
• Use HTML5 validation for all forms.
• Make the search form functional by reading data from PM_DATA in data.js and displaying results in a table.
• Optionally add alert messages or confirmations after forms are submitted.

Person 3 – Aljoharah (CSS)
• Edit assets/css/styles.css.
• Add CSS variables (colors, font sizes, border radius).
• Use Flexbox for the navbar, footer, and quick-actions section.
• Use CSS Grid for the features and plan cards.
• Style all pages to have spacing, padding, rounded cards, and consistent colors.
• Style buttons (.btn) with background color, hover effects, and rounded corners.
• Style forms (labels, inputs, and submit buttons).
• Make the layout responsive using media queries for mobile, tablet, and desktop.
• Highlight the active navigation link (.is-active).
• Add small design touches (card shadows, hover transitions).
